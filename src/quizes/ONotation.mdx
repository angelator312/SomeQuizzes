<Quiz>
  <Quiz.Question>
      ```cpp
      void f(int n) {
        vector<int> a(n);
        for (auto &e : a) cin >> e;
        for (auto &e : a) cout << e << " ";
        cout << endl;
      }
      ```
      <Quiz.Answer>
          $\mathcal{O}$(n log n)
          <Quiz.Explanation>
          Това откъде го взе?
          </Quiz.Explanation>
      </Quiz.Answer>
      <Quiz.Answer correct>
          $\mathcal{O}$(n)
      </Quiz.Answer>
      <Quiz.Answer>
          $\mathcal{O}$(2n)
          <Quiz.Explanation>
          Напълно правилно, но константите се изпускат!
          </Quiz.Explanation>
      </Quiz.Answer>
  </Quiz.Question>
  <Quiz.Question>
      ```cpp
      void f(int n) {
        vector<int> a(n);
        for (auto &e : a) cin >> e;
        for (auto &e : a) cout << e << " ";
        cout << endl;
      }
      void f2(int x)
      {
        for(int i=0;i<x;++i)f(x);
      }
      ```
      Намерете сложността на $f2$
      <Quiz.Answer>
          $\mathcal{O}$(n log n)
          <Quiz.Explanation>
          Това откъде го взе?
          </Quiz.Explanation>
      </Quiz.Answer>
      <Quiz.Answer >
          $\mathcal{O}$(n)
          <Quiz.Explanation>
              Умножаваме x*сложността на $f$
          </Quiz.Explanation>
      </Quiz.Answer>
      <Quiz.Answer correct>
          $\mathcal{O}(n^2)$
      </Quiz.Answer>
  </Quiz.Question>
    <Quiz.Question>
      ```cpp
      int digits(long long a) {
        int len = 0;
        while (a > 0) a /= 10, len++;
        return len;
      }
      ```
      Намерете сложността на $digits$
      <Quiz.Answer correct>
          $\mathcal{O}$(n $log_{10}$ n)
      </Quiz.Answer>
      <Quiz.Answer >
          $\mathcal{O}$(n)
          <Quiz.Explanation>
              По-малко(логаритмите се запазват)
          </Quiz.Explanation>
      </Quiz.Answer>
      <Quiz.Answer>
          $\mathcal{O}(n^2)$
          <Quiz.Explanation>
          Това откъде го взе?
          </Quiz.Explanation>
      </Quiz.Answer>
  </Quiz.Question>
<Quiz.Question>
    ```cpp
    #include <iostream>
    using namespace std;
    using num = long long;
    const long long R = 1e14;
    num n, time_to_next_paner, time_to_get_paner;
    bool IsPossible(num most_used_time) {
    num br = 0, time_to_now = time_to_next_paner;
    while (time_to_now <= most_used_time && br < n) {
        br += (most_used_time - time_to_now) / time_to_get_paner + 1;\
        time_to_now += time_to_next_paner;
    }
    return br >= n;
    }
    void SolveRequest() {
    cin >> n >> time_to_next_paner >> time_to_get_paner;
    num left_pointer = 0, right_pointer = R, ans = -1;
    while (left_pointer <= right_pointer) {
        num mid_pointer = left_pointer + (right_pointer - left_pointer) / 2;
        if (IsPossible(mid_pointer)) {
        right_pointer = mid_pointer - 1;
        ans = mid_pointer;
        } else {
        left_pointer = mid_pointer + 1;
        }
    }
    cout << ans << endl;
    }
    int main() {
    num t;
    cin >> t;
    for (num i = 0; i < t; ++i)
        SolveRequest();
    }
    ```
    Намерете сложността на $SolveRequest$
    <Quiz.Answer correct>
        $\mathcal{O}$(R log n)
        <Quiz.Explanation>
            Няма нищо като for(i=1..R) 
        </Quiz.Explanation>
    </Quiz.Answer>
    <Quiz.Answer>
        $\mathcal{O}$(n log R)
    </Quiz.Answer>
    <Quiz.Answer>
        $\mathcal{O}$(R log R)
        <Quiz.Explanation>
        Това откъде го взе?
        </Quiz.Explanation>
    </Quiz.Answer>
  </Quiz.Question>
</Quiz>
